const { EmployeeProjectRelation } = require("../models");

// exports.addEmpProject = async (req, res, next) => {
//     const project = req.body;
//     console.log(req.body);
//     await Project.create(project);
//     res.status(201).json({ msg: "create project" });
// };
